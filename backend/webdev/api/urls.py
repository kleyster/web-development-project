from django.contrib import admin
from django.urls import path,include

urlpatterns = [
    # path('', include('api.urls')),
    # path('login/',),
    # path('categories/<int:category_id>',)
    # path('categories/<int:category_id>/<int:product_id>',)
]
